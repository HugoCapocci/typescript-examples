function isPair(nombre: number): boolean {
  return nombre % 2 === 0;
}

// console.log('2 est pair ? ', isPair(2));
// console.log('void est pair ? ', isPair());
// console.log('"2" est pair ? ', isPair('2'));

// let dummy: string;
// dummy = isPair(3);

function display(value: string, format: boolean = true) {
}
display('bob');
display('bob', true);


